Language.getInstance(this.requestId)
Language.getInstance(this.requestId).getText('site/title/' + action, params);


this.db.getBySql(_sql);
this.error(ErrorCodes.ERROR_FUNCTION_ARGUMENTS, 'country code [' + code + ']', undefined, false);
this.getFromRequest(name, required = true)

this.getLanguage()
this.getText(adress, vars)
this.getServiceName()


* @param {boolean} needResult - is result required



const Cache = require('application/express/components/base/Cache');
const ErrorCodes = require('application/express/settings/ErrorCodes');
const Language = require('application/express/components/base/Language');







for (var index in arr) {
    arr[index] = toInt(arr[index]);
}










