/*
 * File application/express/settings/DebugSettings.js
 * const DebugSettings = require('application/express/settings/DebugSettings');
 *
 * Settings for fast debugging
 * Here we can turn off some modules to speed up compilation
 */

module.exports = {
    ftp: 0
}