/*
 * File application/express/settings/gitignore/Upload.js
 *
 * Upload files server settings
 */
const Consts = require('application/express/settings/Constants');

module.exports = {
    // Which server will be used for keeping files (ftp or local)
    server:'ftp'
}