/*
 * File server/src/settings/DebugSettings.js
 * const DebugSettings = require('server/src/settings/DebugSettings');
 *
 * Settings for fast debugging
 * Here we can turn off some modules to speed up compilation
 */

module.exports = {
    ftp: 0
}